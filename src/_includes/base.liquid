<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{ pageTitle | default: "trauko's comfy lair" }}</title>
        <link  rel="stylesheet" href="{{ 'assets/css/styles.css' }}">
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="{{ 'assets/js/WindowManager.js' }}" type="module"></script>
    </head>
    <body>
        {{ content }}
    </body>
</html>